<p id="notice"><%= notice %></p>

<h1>Listing Teams</h1>

<p> Click on a team's number to go to their page where you can upload feedback and pics </p>

<table>
  <thead>
    <tr>
      <th>Team number</th>
      <th>Nickname</th>
      <th>Location</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= link_to team.team_number, team %></td>
        <td><%= team.nickname %></td>
        <td><%= team.location %></td>
        <td><%= link_to 'Edit', edit_team_path(team) %></td>
        <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path %>
